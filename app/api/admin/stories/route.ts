import { storySchema } from '../../../../db/schema';

const json=(body:unknown,status=200)=>Response.json(body,{status,headers:{'Cache-Control':'no-store'}});
async function context(){try{const {env}=await import('cloudflare:workers');const db=env.DB as D1Database;await db.batch(storySchema.map(sql=>db.prepare(sql)));return {db,adminIds:new Set(String(env.ADMIN_USER_IDS??'').split(',').filter(Boolean))};}catch{return null;}}
function currentId(request:Request){return request.headers.get('oai-authenticated-user-id');}
export async function GET(request:Request){const ctx=await context(),id=currentId(request);if(!ctx)return json({admin:false,stories:[],localPreview:true});if(!id||!ctx.adminIds.has(id))return json({error:'운영자 권한이 필요합니다.'},403);const result=await ctx.db.prepare(`SELECT id,nickname,title,body,goal,amount,period,tag,status,featured,report_count AS reportCount,created_at AS createdAt FROM stories ORDER BY CASE status WHEN 'pending' THEN 0 WHEN 'hidden' THEN 1 ELSE 2 END, created_at DESC LIMIT 100`).all();return json({admin:true,stories:result.results});}
export async function PATCH(request:Request){const ctx=await context(),userId=currentId(request);if(!ctx)return json({error:'로컬 미리보기에서는 사용할 수 없습니다.'},503);if(!userId||!ctx.adminIds.has(userId))return json({error:'운영자 권한이 필요합니다.'},403);const input=await request.json() as {id?:string;action?:'approve'|'hide'|'feature'|'unfeature'};if(!input.id||!input.action)return json({error:'잘못된 요청입니다.'},400);if(input.action==='approve')await ctx.db.prepare(`UPDATE stories SET status='approved' WHERE id=?`).bind(input.id).run();if(input.action==='hide')await ctx.db.prepare(`UPDATE stories SET status='hidden' WHERE id=?`).bind(input.id).run();if(input.action==='feature')await ctx.db.prepare(`UPDATE stories SET status='approved',featured=1 WHERE id=?`).bind(input.id).run();if(input.action==='unfeature')await ctx.db.prepare(`UPDATE stories SET featured=0 WHERE id=?`).bind(input.id).run();return json({message:'처리되었습니다.'});}
