'use client';
import { FormEvent, useState } from 'react';
type Profile={authenticated:boolean;nickname?:string;email?:string};
export default function ProfileModal({profile,onSaved,onClose}:{profile:Profile;onSaved:(p:Profile)=>void;onClose:()=>void}){const [nickname,setNickname]=useState(profile.nickname??'');const [error,setError]=useState('');if(!profile.authenticated)return <div className="modal-backdrop"><section className="modal-sheet profile-sheet"><header><div><span>ACCOUNT</span><h2>로그인하고 계속하기</h2><p>후기와 닉네임을 내 계정에 안전하게 연결해요.</p></div><button onClick={onClose}>×</button></header><div className="signin-box"><span>ㅊ</span><h3>참았다! 계정 연결</h3><p>ChatGPT 계정으로 로그인하면 내 프로필을 안전하게 관리할 수 있어요.</p><a href="/signin-with-chatgpt?return_to=%2F">ChatGPT로 로그인</a></div></section></div>;const submit=async(e:FormEvent)=>{e.preventDefault();const r=await fetch('/api/profile',{method:'PUT',headers:{'Content-Type':'application/json'},body:JSON.stringify({nickname})});const p=await r.json();if(!r.ok)return setError(p.error??'저장하지 못했어요.');onSaved({...profile,nickname:p.nickname})};return <div className="modal-backdrop"><section className="modal-sheet profile-sheet"><header><div><span>ACCOUNT</span><h2>내 프로필</h2><p>후기와 이벤트에 표시할 이름을 정해주세요.</p></div><button onClick={onClose}>×</button></header><form className="profile-form" onSubmit={submit}><div className="profile-avatar">🙂</div><small>{profile.email}</small><label><span>닉네임</span><input autoFocus minLength={2} maxLength={16} required value={nickname} onChange={e=>setNickname(e.target.value)} placeholder="2~16자"/></label>{error&&<p>{error}</p>}<button className="submit-button">프로필 저장</button><a href="/signout-with-chatgpt?return_to=%2F">로그아웃</a></form></section></div>}
